library identifier: 'Aprende_Jenkins@main',
        retriever: modernSCM([$class: 'GitSCMSource', remote: 'https://github.com/ibmug/Aprende_Jenkins.git'])

pipeline {
    agent any
    stages {
        stage('Audit tools') {
            steps {
                sendMessage message: 'PirataGomo sending his regards to the crew'
            }
        }
    }
}
